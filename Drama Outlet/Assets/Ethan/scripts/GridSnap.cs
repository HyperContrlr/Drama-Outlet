using UnityEngine;public class GridSnap : MonoBehaviour{private Grid grid;void Start(){grid = Grid.FindFirstObjectByType<Grid>(); } void Update() { Vector3Int yurr = grid.LocalToCell(transform.localPosition); transform.localPosition = grid.GetCellCenterLocal(yurr); }}
